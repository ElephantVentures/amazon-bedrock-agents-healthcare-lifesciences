# Environment variables for PubMed Multi-Agent System Deployment
# Copy this file to .env and fill in your values

# Required Variables
# ==================

# S3 bucket for deployment artifacts (must exist and be accessible)
BUCKET_NAME=your-deployment-bucket-name

# AWS region for deployment
REGION=us-east-1

# Environment name for resource naming (lowercase alphanumeric only)
ENVIRONMENT_NAME=prod

# AWS Credentials
# ===============
# Option 1: Use AWS credentials directly
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_SESSION_TOKEN=your-session-token  # Only needed for temporary credentials

# Option 2: Use AWS profile (alternative to direct credentials)
# AWS_PROFILE=your-aws-profile-name

# Optional Variables
# ==================

# Pre-existing IAM role for Bedrock agents (if not provided, will be created)
# BEDROCK_AGENT_SERVICE_ROLE_ARN=arn:aws:iam::123456789012:role/service-role/AmazonBedrockExecutionRoleForAgents_xyz

# NCBI API key for enhanced rate limits (recommended for production)
# Get your key from: https://ncbiinsights.ncbi.nlm.nih.gov/2017/11/02/new-api-keys-for-the-e-utilities/
# NCBI_API_KEY=your-ncbi-api-key

# Bedrock Model Configuration
# ===========================
# Primary model for agents (default: amazon.nova-pro-v1:0)
# BEDROCK_MODEL_ID=amazon.nova-pro-v1:0

# Docker Configuration
# ====================
# Set to 'true' for interactive debugging
# DOCKER_INTERACTIVE=false

# Deployment Options
# ==================
# Set to 'true' to skip Lambda function updates after deployment
# SKIP_LAMBDA_UPDATE=false

# Set to 'true' to validate deployment after completion
# VALIDATE_DEPLOYMENT=true
