# Deployment dependencies for PubMed Multi-Agent System
# These are used for building Lambda layers and deployment scripts

# Core AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# Lambda function dependencies (for layer creation)
httpx==0.27.0
defusedxml==0.7.1

# Deployment utilities
pyyaml>=6.0
requests>=2.31.0

# Development and testing
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
